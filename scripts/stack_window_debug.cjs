const fs=require('fs');const s=fs.readFileSync('src/App.jsx','utf8');let stack=[];let inStr=null;for(let i=0;i<s.length;i++){const ch=s[i];if(inStr){if(ch==='\\'){i++;continue;} if(ch===inStr) inStr=null; continue;} if(ch==='"'||ch==="'"||ch==='`'){inStr=ch;continue;} if(ch==='('||ch==='{'||ch==='[') stack.push({ch,i}); else if(ch===')'||ch==='}'||ch===']'){ if(stack.length===0){ console.log('Unmatched closing',ch,'at',i); process.exit(1);} const top=stack[stack.length-1]; if((top.ch==='('&&ch===')')||(top.ch==='{'&&ch==='}')||(top.ch==='['&&ch===']')) stack.pop(); else{ console.log('MISMATCH at',i,'top',top,'found',ch); console.log('Context:', s.slice(i-120,i+40)); console.log('Stack:', stack); process.exit(1);} } if(i>145900 && i<146200){ if((i%1)===0){} } if(i===146121){ console.log('At 146121, stack:',stack); console.log('Context:', s.slice(i-200,i+40)); } } console.log('done');
