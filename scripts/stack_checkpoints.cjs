const fs=require('fs');const s=fs.readFileSync('src/App.jsx','utf8');let stack=[];let inStr=null;const cps=[140000,141000,142000,143000,144000,145000,146000];let idx=0;for(let i=0;i<s.length;i++){const ch=s[i];if(inStr){if(ch==='\\'){i++;continue;} if(ch===inStr) inStr=null; continue;} if(ch==='"'||ch==="'"||ch==='`'){inStr=ch;continue;} if(ch==='('||ch==='{'||ch==='[') stack.push({ch,i}); else if(ch===')'||ch==='}'||ch===']'){ if(stack.length===0){ console.log('Unmatched closing',ch,'at',i); console.log('At checkpoint',idx,'i',i); process.exit(1);} const top=stack[stack.length-1]; if((top.ch==='('&&ch===')')||(top.ch==='{'&&ch==='}')||(top.ch==='['&&ch===']')) stack.pop(); else{ console.log('Mismatch at',i,'top',top,'found',ch); process.exit(1);} } if(i===cps[idx]){ console.log('checkpoint',cps[idx],'stacklen',stack.length); idx++; if(idx>=cps.length) break;} }console.log('done');
